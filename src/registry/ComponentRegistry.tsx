/**
 * Re-export from engine for backward compatibility.
 * New code should import from @/engine/registry directly.
 */

export { getComponent, registerComponent, getRegisteredTypes } from '@/engine/registry';
